%h2= @player.name
= render "leaderboard/table", rows: [@leaderboard_row] if @leaderboard_row
.row
  %h3= t(:games)
  = render "games/list", games: @player.games, player: @player
.row
  %h3= t(:achievements)
  = render "achievements/grid", achievements: @achievements, player: @player
